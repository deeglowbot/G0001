var triggerList=new Array();

var ITEM=1;

function Trig_Add()
{
	var index=triggerList.length;
	triggerList[index]=new Object();
	//complete
	//active
	//inactive

	//type
	//message , the message displayer needs to
}